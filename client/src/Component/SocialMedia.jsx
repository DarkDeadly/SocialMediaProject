import { Button } from '@/components/ui/button'
import React from 'react'

const SocialMedia = () => {
  return (
    <div>
      <Button className={'cursor-pointer w-full'}>Continue With Google</Button>
    </div>
  )
}

export default SocialMedia
